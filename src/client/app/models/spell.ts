export interface SpellVar {
    ranksWith: string;
    dyn: string;
    link: string;
    coeff: number[];
    key: string;
}

export interface SpellLevelTip {
    effect: string[];
    label: string[];
}
